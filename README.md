
**This is a library for tree functions I make during the learning process of the tree data structure**

Will prob have edge cases I don't even know exist

**every single .hpp is in /headers and .cpp's for them are in /src**
Compile using bash:
    ``
    g++ main.cpp src/print_tree.cpp src/node_class.cpp src/equations -o main -I.
    ``
