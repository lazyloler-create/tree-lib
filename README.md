
**This is a library for tree functions I make during the learning process of the tree data structure**
Will prob have edge cases I don't even know exist

*node_class.hpp holds the node class and createNode(), .cpp is in src/node_class.cpp*
*print_tree.hpp holds the function prototypes for printing tree's in various ways, cpp is in src/print_tree.cpp*

Compile using bash:
    ``
    g++ main.cpp src/print_tree.cpp src/node_class.cpp -o main -I.
    ``